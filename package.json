{
  "name": "sparks",
  "version": "0.1.0",
  "private": true,
  "description": "Sparks desktop — idle compute earns lottery entries for USDT/BTC (winbitcoin.app)",
  "workspaces": [
    "apps/desktop"
  ],
  "scripts": {
    "dev": "npm run tauri:dev -w @sparks/desktop",
    "build": "npm run tauri:build -w @sparks/desktop",
    "fetch-worker": "npm run fetch-worker -w @sparks/desktop",
    "start": "npm run fetch-worker && npm run dev"
  },
  "engines": {
    "node": ">=20"
  }
}
