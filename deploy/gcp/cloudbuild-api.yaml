steps:
  - name: gcr.io/cloud-builders/docker
    args:
      - build
      - -f
      - apps/api/Dockerfile
      - -t
      - ${_IMAGE}
      - -t
      - ${_LATEST}
      - .
images:
  - ${_IMAGE}
  - ${_LATEST}
