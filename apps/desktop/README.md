# Sparks Desktop (Tauri 2)

Prefer the root [install.sh](../../install.sh) / root README.

```bash
# from repo root
npm install
npm run fetch-worker
npm run dev
```

Default API is production Cloud Run. Override in Settings → Advanced for local testing.
